/**
 * Imports
 */
const config = require("./config.json");
const text = require("./text.json");
const block = require("./block.json");

/**
 * Export
 */
module.exports = {
  config,
  text,
  block
};
